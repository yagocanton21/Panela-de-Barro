# Força LF em scripts shell e entrypoints — evita erro $'\r' no Linux/WSL/Docker
*.sh   text eol=lf
*.bash text eol=lf
entrypoint.sh text eol=lf

# Normaliza fins de linha de texto no repo
* text=auto
